# SIGNALD

Install packages

conda install conda-forge::plant
conda install hcc::adfr-suite
conda install conda-forge::pymol-open-source

Usage
Copy ligands in .sdf format into ligand folder

Perform PLANTS docking
python 1.1_run_plants.py -receptor receptor.mol2 -center (x, y, z) 
